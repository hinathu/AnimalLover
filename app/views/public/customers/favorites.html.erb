<div class="container">
   <%= render 'public/header' %>
   
   <div class="row mt-5">
    <h3><%= @customer.name %>さんのお気に入り一覧</h3>
  </div>
  
  <div class="row mt-3">
    <% @favorite_items.each do |item|%>
      <%= item.name %>
    <% end %>
  </div>
  
</div>